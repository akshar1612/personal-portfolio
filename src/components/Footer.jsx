export default function Footer() {
  return (
    <footer>
      <a href="#hero" style={{ fontSize: "0.7rem", letterSpacing: "0.12em", textTransform: "uppercase", color: "var(--fg-dim)" }}>
        Back to top ↑
      </a>
    </footer>
  );
}
